
%h3 #{fuda.front} Front/Back
= link_to cookies[:random_url] do
  .fuda
    .row
      .pull-left
        %h4= fuda.id
      .pull-right
        %h4= yamafuda.name
    .row
      .span1 &nbsp;
    .row
      .text-center
        .front= simple_format fuda.front
        -# %h2= fuda.kanji.flat_map(&:radicals).map(&:literal).join(' ')

= link_to cookies[:random_url] do
  .fuda
    .row
      .pull-right
        %h4= yamafuda.name
    .row
      .span1 &nbsp;
    .row
      .text-center
        .back= simple_format fuda.back

